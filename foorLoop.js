/*2. Write a for loop to iterate over an array of numbers. Use let to declare the loop variable as it
needs to be re-assigned for each iteration.*/

let arr=[1,2,3,4,5,6,7,8]
for(let i=0;i<arr.length;i++){
    //let arrs=arr[i]
  console.log(arr[i])
}